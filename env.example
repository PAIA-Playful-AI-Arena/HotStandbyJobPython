# HotStandbyJob Operator 環境變數配置範例
# 複製此檔案為 .env 並根據您的環境修改

# ============================================
# Operator 基本配置
# ============================================

# Sync interval (seconds) - Operator 定期同步的間隔
SYNC_INTERVAL=10.0

# ============================================
# Redis 配置（用於 Redis 模式）
# ============================================

# Redis 主機地址
# - Kubernetes 內：使用 Service 名稱，例如 "redis-service"
# - 本地開發：使用 "localhost" 或 Redis 容器名稱
REDIS_HOST=redis-service

# Redis 埠號（預設 6379）
REDIS_PORT=6379

# Redis 資料庫編號（預設 0）
REDIS_DB=0

# Redis Key 前綴（用於區分不同的應用）
# 預設值：pod-status（會建立 key: mlgame-daemon-pod）
REDIS_KEY_PREFIX=mlgame-daemon-pod

# Redis 密碼（如果需要認證）
# 留空表示不需要密碼
REDIS_PASSWORD=

# Redis 連線超時（秒）
REDIS_CONNECT_TIMEOUT=5

# Redis TTL（秒）- Pod 狀態的過期時間
REDIS_TTL_SECONDS=300



# ============================================
# 日誌配置
# ============================================

# 日誌等級：DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ============================================
# 功能開關
# ============================================

# 是否啟用 Redis 模式（預設為自動偵測）
# true: 強制啟用（如果 Redis 連接失敗會報錯）
# false: 強制停用（即使有 Redis 連接也不使用）
# auto: 自動偵測（預設值，有 Redis 就用，沒有就 fallback）
ENABLE_REDIS_MODE=auto

